{
  "name": "runmate-backend-deploy",
  "version": "1.0.0",
  "description": "RunMate backend deployment",
  "scripts": {
    "start": "cd backend && npm start",
    "install": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 