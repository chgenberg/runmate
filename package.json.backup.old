{
  "name": "runmate-backend-deploy",
  "version": "1.0.0",
  "description": "RunMate backend deployment",
  "scripts": {
    "start": "echo 'Starting backend...' && cd backend && echo 'Changed to backend directory' && ls -la && echo 'About to run npm start' && npm start",
    "install": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 